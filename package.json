{
  "name": "youtube-control-room",
  "version": "0.4.0",
  "private": true,
  "description": "Local multilingual YouTube planning and production control room",
  "engines": { "node": ">=24.0.0 <25" },
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check server.js && node --check store.js && node --check youtube.js && node --check connectors.js && node --check hermes.js && node --check public/app.js",
    "tracker": "node scripts/tracker.js"
  }
}
